---
"@coding-flow/flow-design": minor
---

子流程节点配置支持手动传入子流程标题（workTitle）：在子流程配置弹窗每个子流程配置项新增"子流程标题"输入框，配置后生成 5 参 `toCreateRequest(..., workTitle)` 脚本调用；未配置时保持 4 参调用兼容旧脚本。创建的子流程实例（含审批/后续节点记录）以自定义标题展示，不填则回落流程定义标题（issue #197）。